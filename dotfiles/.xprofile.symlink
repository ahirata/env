xrdb -merge ~/.config/solarized/xterm

export GTK2_RC_FILES=~/.gtkrc-2.0

export XDG_CONFIG_HOME=~/.config

# force GTK to use XIM (.XCompose) in order to enable ç/Ç in gtk
export GTK_IM_MODULE=xim

# silence gtk warning - Couldn't register with accessibility bus: Did not receive a reply
export NO_AT_BRIDGE=1

